(function(Global) {
  'use strict';

  // in Meteor, Global.Phaser and Phaser being a global variable is not the same thing
  Phaser = Global.Phaser;

  // define PhaserExtensions namespace
  PhaserExtensions = {};
})(this);